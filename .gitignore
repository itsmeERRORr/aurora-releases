# macOS
.DS_Store

# Xcode user state
*.xcuserstate
xcuserdata/

# Build output
build/
DerivedData/

# Publish output — DMGs and appcast (Phase 3+).
# We DO commit `releases/appcast.xml` later (Phase 4 publishes it to GitHub
# Releases / a CDN). Until then it's local-only.
releases/*.dmg
releases/.private/
releases/Aurora-*

# Local env
.env.local
